fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.30
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0
python-multipart==0.0.9
# Screenshot -> signal extraction (vision): official Anthropic SDK + Pillow for
# the image preprocessing (upscale + price-axis crop) that makes levels legible.
anthropic>=0.92.0
Pillow>=10.0.0
# Test-only (not needed to run the backend). Version ranges so a stock macOS
# Python 3.9 can install them too — pytest-asyncio 1.3+ requires Python 3.10+.
pytest>=8.2,<9
pytest-asyncio>=0.23,<2
