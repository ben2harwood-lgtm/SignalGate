# Hosted/server deployment requirements.
# Installs local backend dependencies plus the PostgreSQL driver and migration tool.
# Hosted schema changes are Alembic-managed; the application never runs create_all().
-r requirements.txt
psycopg2-binary==2.9.13
alembic==1.20.0


# Container/runtime hardening pins.
# Trivy identified vulnerable versions in the built hosted image; these are the
# first fixed versions reported for the corresponding findings.
setuptools==83.0.0
msgpack==1.2.1
